//////math /////////////

sin()    | سینوس یک عدد را بر میگرداند 
cos()    | کوسینوس یک عدد را برمیگرداند
tan()    | تانژانت یک عدد را محاسبه میکند 
fmod()   | باقی مانده تقسیم دو عدد را برمیگرداند 
hexdex() | یک عدد مبنای 16 را به مبنای 10 تبدیل میکند 
log()    |لگاریتم طبیعی یک عدد را برمیگرداند 
pi()     | عدد پی را برمیگرداند
acos()   | آرک کسینوس یک عدد را برمیگرداند 
asin()   | آرک سینوس یک عدد را برمیگرداند 


///////string ///////////////////

strcmp()             | دو رشته را باهم مقایسه میکند 
str_replace()        | یک بخش از رشته را جایگزین میکند 
trim()               | کاراکتر های مشخص شده و فضای خالی را از طرفین رشته حذف میکند 
ucfirst()            |  اولین کاراکتر از رشته را به حروف بزرگ تبدیل میکند 
ucwords()            | اولین کاراکتر از هر کلمه رشته را به حروف بزرگ تبدیل میکند 
chop()               | فضای خالی یک رشته را حذف میکند 
sprint()             |یک رشته را فرمت دهی شده را داخل متغییر ذخیره میکند 
htmlentities()       | کد اچ تی ام ال را درون یک رشته ، کد گذاری میکند 
html_entity_decode() |کد های اچ تی ام ای را از درون رشته بازیابی میکند 